export default function FavoriteRecipesComponent(recipeList) {
  return (
    <div className="favoriteRecipes">
      <h2>Favorite Recipes</h2>
      {/* {recipeList.map((recipe, index) => (
        <div>
          <ul>
            <li key={index}>{recipe.title}</li>
          </ul>
        </div>
      ))} */}
    </div>
  );
}
